# snippet - A cryptographically verifyable pastebin service.
snippet is a self-contained PHP action page that takes a user's content and creates a SHA256 sum from it. This SHA256 sum is used to name the file that will store the content, and is used as a reference in the URL.
